import { HttpClientEnvConfig } from '@libs/common/external/http/interface/http-client.interface';

/**
 * Integration 앱에서 사용하는 HTTP Client 설정 모음
 */
export const integrationHttpConfigs: HttpClientEnvConfig[] = [
  /* empty */
];
